import React from 'react';

const CommentList = (props) => {
  return (
    <div className="CommentList" data-testid="CommentList">
      {/* Here comes the content of CommentList */}
    </div>
  );
};

export default CommentList;
